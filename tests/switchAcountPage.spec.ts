// import {test, expect} from '@playwright/test';
// import {userFactory} from '../utils/data';

// test('go to switch Page without login', async ({ page }) => {
//   await page.goto('https://goto.your.rentals/organisation/switch-account');
//   await expect(page.getByRole('heading', { name: 'Welcome back!'})).toBeVisible();
// });
// const user = userFactory.create({ firstName: 'Manager' });